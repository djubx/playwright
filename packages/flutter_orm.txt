

// The app starts by initializing the database using the `Database.init()` method.
// This method returns a future that completes when the database is ready to use.
// We then create a new user and insert it into the database using the `orm.insert()` method.
// Next, we retrieve all users from the database using the `orm.select()` method and print them to the console.
// We then update the user's name and update the database using the `orm.update()` method.
// Finally, we delete the user from the database using the `orm.delete()` method.
// The app then runs the `MyApp` widget, which displays a simple "Hello, World!" message.

<-- END_MAIN -->