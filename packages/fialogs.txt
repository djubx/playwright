

// This is the main entry point of the app.
// The app starts by running the `main` function, which creates a new instance of the `MyApp` widget.
// The `MyApp` widget is the root of the app's widget tree.
// It creates a new instance of the `RealFlutter` widget, which is the main content of the app.
// The `RealFlutter` widget creates a new instance of the `Scaffold` widget, which provides a basic material design layout structure.
// The `Scaffold` widget creates a new instance of the `AppBar` widget, which displays the app's title.
// The `Scaffold` widget also creates a new instance of the `Center` widget, which centers its child widget.
// The `Center` widget creates a new instance of the `Column` widget, which displays its children in a vertical column.
// The `Column` widget creates two new instances of the `ElevatedButton` widget, which display the "Show Dialog" and "Show Alert" buttons.
// When the "Show Dialog" button is pressed, the app shows a new instance of the `AlertDialog` widget, which displays a custom dialog.
// When the "Show Alert" button is pressed, the app shows a new instance of the `AlertDialog` widget, which displays a custom alert.

<-- END_MAIN -->