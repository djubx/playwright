

// This is the main entry point of the application.
// The `MyApp` widget is the root of the widget tree.
// It uses the `MaterialApp` widget to create a Material Design app.
// The `RealFlutter` widget is the home screen of the app.

// The `RealFlutter` widget uses the `HtmlViewer` widget to display the HTML content.
// The `HtmlViewer` widget is created with the `_htmlContent` string, which contains the HTML code.
// The `onViewCreated` callback is used to interact with the HTML viewer.

// When the app is run, the `HtmlViewer` widget will render the HTML content.
// The user can interact with the HTML viewer, such as scrolling and clicking on links.

// This demo showcases the basic usage of the `flutter_super_html_viewer` package.
// It demonstrates how to display HTML content in a Flutter app using the `HtmlViewer` widget.

<-- END_MAIN -->