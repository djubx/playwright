

// The code flow is as follows:
// 1. Import the necessary packages, including `flutter` and `alquran_cloud`.
// 2. Create a `RealFlutter` widget that extends `StatefulWidget`.
// 3. In the `_RealFlutterState` class, create an instance of `AlquranApi` and a list to store the surahs.
// 4. In the `initState` method, call the `_fetchSurahs` function to fetch the surahs.
// 5. In the `_fetchSurahs` function, use the `AlquranApi` instance to fetch the surahs and then update the `_surahs` list.
// 6. In the `build` method, create a `MaterialApp` widget with a `Scaffold` widget as its child.
// 7. In the `Scaffold` widget, create an `AppBar` widget with a title and a `ListView.builder` widget to display the surahs.
// 8. In the `ListView.builder` widget, create a `ListTile` widget for each surah, displaying its name and English name.

<-- END_MAIN -->