
<-- END_MAIN -->

In this example, the app uses the `aws_polly_api` package to synthesize text into speech. The user can enter text and select a voice from the dropdown menu. When the user presses the "Synthesize Speech" button, the app uses the AWS Polly client to synthesize the text into speech. The app then plays the audio or saves it to a file.