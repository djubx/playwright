<-- START_DESCRIPTION -->

# gt3_flutter_plugin Package Overview
=====================================

The `gt3_flutter_plugin` package is a Flutter plugin that provides a simple and efficient way to integrate Google's reCAPTCHA v3 into your Flutter application. reCAPTCHA v3 is a free service offered by Google that helps protect your website or application from spam and abuse.

### When to Use This Package

You should use this package when you need to protect your Flutter application from spam and abuse. This package is particularly useful when you have forms or other user-input fields that need to be protected from automated scripts.

### Features

*   Simple and easy-to-use API
*   Supports both Android and iOS platforms
*   Provides a flexible way to customize the reCAPTCHA v3 widget

<-- END_DESCRIPTION -->

<-- START_TUTORIAL -->

# Setting Up the gt3_flutter_plugin Package
==========================================

To use the `gt3_flutter_plugin` package, you need to add it to your `pubspec.yaml` file. Here's how you can do it:

### Step 1: Add the Package to Your pubspec.yaml File

```yml
dependencies:
  flutter:
    sdk: flutter
  gt3_flutter_plugin: ^1.0.0
```

### Step 2: Run the `flutter pub get` Command

After adding the package to your `pubspec.yaml` file, run the `flutter pub get` command in your terminal to get the package.

### Step 3: Import the Package in Your Dart File

```dart
import 'package:gt3_flutter_plugin/gt3_flutter_plugin.dart';
```

### Platform-Specific Details

#### Android

To use the `gt3_flutter_plugin` package on Android, you need to add the following code to your `AndroidManifest.xml` file:

```xml
<application
    ...
    android:usesCleartextTraffic="true">
    ...
</application>
```

#### iOS

To use the `gt3_flutter_plugin` package on iOS, you need to add the following code to your `Info.plist` file:

```xml
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <true/>
</dict>
```

### Required Configurations or Optimizations

To use the `gt3_flutter_plugin` package, you need to configure the reCAPTCHA v3 API keys. You can do this by calling the `Gt3FlutterPlugin.configure` method:

```dart
Gt3FlutterPlugin.configure(
  apiKey: 'YOUR_API_KEY',
  apiSecretKey: 'YOUR_API_SECRET_KEY',
);
```

<-- END_TUTORIAL -->

<-- START_MAIN -->

```dart
import 'package:flutter/material.dart';
import 'package:gt3_flutter_plugin/gt3_flutter_plugin.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'gt3_flutter_plugin Demo',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: MyHomePage(),
    );
  }
}

class MyHomePage extends StatefulWidget {
  @override
  _MyHomePageState createState() => _MyHomePageState();
}

class _MyHomePageState extends State<MyHomePage> {
  final _formKey = GlobalKey<FormState>();
  final _textFieldController = TextEditingController();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('gt3_flutter_plugin Demo'),
      ),
      body: Padding(
        padding: const EdgeInsets.all(20.0),
        child: Form(
          key: _formKey,
          child: Column(
            children: [
              TextFormField(
                controller: _textFieldController,
                decoration: InputDecoration(
                  labelText: 'Enter your name',
                  border: OutlineInputBorder(),
                ),
                validator: (value) {
                  if (value!.isEmpty) {
                    return 'Please enter your name';
                  }
                  return null;
                },
              ),
              SizedBox(height: 20),
              ElevatedButton(
                onPressed: () async {
                  if (_formKey.currentState!.validate()) {
                    // Call the Gt3FlutterPlugin.verify method to verify the user
                    final result = await Gt3FlutterPlugin.verify();
                    if (result) {
                      // If the user is verified, display a success message
                      ScaffoldMessenger.of(context).showSnackBar(
                        SnackBar(content: Text('You are verified!')),
                      );
                    } else {
                      // If the user is not verified, display an error message
                      ScaffoldMessenger.of(context).showSnackBar(
                        SnackBar(content: Text('You are not verified!')),
                      );
                    }
                  }
                },
                child: Text('Verify'),
              ),
            ],
          ),
        ),
      ),
    );
  }
}

// The application flow is as follows:
// 1. The user opens the app and is presented with a form to enter their name.
// 2. The user enters their name and clicks the "Verify" button.
// 3. The Gt3FlutterPlugin.verify method is called to verify the user.
// 4. If the user is verified, a success message is displayed.
// 5. If the user is not verified, an error message is displayed.

// The Gt3FlutterPlugin.verify method uses the reCAPTCHA v3 API to verify the user.
// The reCAPTCHA v3 API uses a combination of factors, such as the user's IP address, browser type, and behavior, to determine whether the user is a human or a bot.

// By using the Gt3FlutterPlugin, we can protect our app from spam and abuse, and ensure that only legitimate users can access our app's features.
```

<-- END_MAIN -->