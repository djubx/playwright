

// The app's widget tree is wrapped with the FeatureFlag widget, which provides
// access to the feature flags.
// 
// The RealFlutter widget uses the FeatureFlag widget to toggle the 'new_feature'
// flag on and off.
// 
// The NewFeature widget is only visible if the 'new_feature' flag is enabled.

// To toggle the 'new_feature' flag, you can use the FeatureFlags class.
// For example:
// 
// final featureFlags = FeatureFlags.of(context);
// featureFlags.enable('new_feature');

// This code enables the 'new_feature' flag, making the NewFeature widget visible.

// You can also use the FeatureFlags class to disable the 'new_feature' flag.
// For example:
// 
// final featureFlags = FeatureFlags.of(context);
// featureFlags.disable('new_feature');

// This code disables the 'new_feature' flag, making the NewFeature widget invisible.

<-- END_MAIN -->