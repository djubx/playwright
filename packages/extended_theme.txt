

// This is the main entry point of the app.
// The `MyApp` widget is created and run.
// The `MyApp` widget creates a theme and a theme switcher.
// The theme switcher is used to switch between themes.
// The `MyHomePage` widget is created and displayed.
// The `MyHomePage` widget gets the current theme from the theme switcher.
// The current theme is used to style the app.

// The app uses the `ExtendedTheme` package to manage themes.
// The `ExtendedTheme` package provides a simple and efficient way to switch between themes.
// The app creates a theme and a theme switcher.
// The theme switcher is used to switch between themes.
// The app uses the current theme to style the app.

<-- END_MAIN -->