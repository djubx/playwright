

// This is the main entry point of the app.
// It creates a Material app with a home page.
// The home page is a stateful widget that displays a list of available Bluetooth devices.
// When a device is selected, it connects to the device and displays the connected device's name.
// The app also has a disconnect button that disconnects from the currently connected device.

// The app uses the BluetoothConnector package to connect to Bluetooth devices.
// It discovers available devices and connects to the selected device.
// The app also handles disconnections and updates the UI accordingly.

// The app's UI is simple and easy to use.
// It displays a list of available devices and allows the user to select a device to connect to.
// Once connected, it displays the connected device's name and allows the user to disconnect.

<-- END_MAIN -->