

// This code creates a new Stripe client with a publishable key.
// It then defines three functions to create a new customer, add a new credit card, and create a new subscription.
// The `createCustomer` function creates a new customer with an email address.
// The `addCard` function adds a new credit card with a card number, expiration month, expiration year, and CVC code.
// The `createSubscription` function creates a new subscription with a customer ID and plan ID.
// The code then builds a simple UI with three buttons to call each of these functions.

<-- END_MAIN -->