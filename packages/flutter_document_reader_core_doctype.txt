

<-- END_MAIN -->

In this example, we create a simple Flutter application that allows the user to select a document using a file picker. The selected document is then processed using the `DocumentReader` class, and the processed document text is displayed on the screen. The application flow is explained in the comments at the end of the code block.