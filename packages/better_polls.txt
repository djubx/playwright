

<-- END_MAIN -->

In summary, the code flows as follows:

*   The user is presented with a poll question and options.
*   The user selects an option and votes.
*   The user's vote is handled and the results are displayed.
*   The user can view the results of the poll.

The code flow is as follows:

*   The poll is created with a question and options.
*   The poll is displayed to the user using the `PollWidget`.
*   The user's vote is handled using the `onVote` callback.
*   The results of the poll are displayed to the user.