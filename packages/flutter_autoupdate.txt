

// The app is launched and the `checkForUpdates` function is called.
// The `checkForUpdates` function checks for updates and shows a dialog if an update is available.
// If the user clicks "Yes", the `downloadAndInstallUpdate` function is called.
// The `downloadAndInstallUpdate` function downloads and installs the update.
// If the user clicks "No", the dialog is closed.

<-- END_MAIN -->